import React from 'react';
import { createRoot } from 'react-dom/client';
import { LoginApp } from './LoginApp.jsx';

createRoot(document.getElementById('root')).render(React.createElement(LoginApp));
